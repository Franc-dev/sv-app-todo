<script lang="ts">
    export let size = 6;
    export let color = 'primary-500';
  </script>
  
  <div class={`w-${size} h-${size} rounded-full animate-spin border-4 border-solid border-${color} border-t-transparent`}></div>